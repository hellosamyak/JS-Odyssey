"use strict"; // to treat all the JS code as newer version

// alert( 3 + 3) *erroneous as we are using node.js, and not browser.

let name = "samyak" //string
let age = 20 //number => 2 to the power 53
let isLoggedIn = true //boolean

// more datatypes
// null => standalone value
// bigint => bigger number, usually used in trading apps
// undefined => value not assigned
// symbol => uniqueness

// object

console.log(typeof 9);
console.log(typeof null);
console.log(typeof undefined);
alert(3 + 3)